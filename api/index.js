import request from '@/common/request.js'

const api = {}

// //获取启动视频
// api.appvideo = params => request.globalRequest(`api/video/index`, 'POST', params)

// //批量上传相册
// api.insertLotImg = params => request.globalUpload(`api/album/insertAll`, 'imgs[]', params)

// //上传相册
// api.insertAlbumImg = params => request.globalUpload(`api/album/create`, 'img', params)



export default api
